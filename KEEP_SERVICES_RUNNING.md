# الحفاظ على تشغيل الخدمات 🔄

## ✅ الحالة الحالية

### PostgreSQL
- **الحالة**: Running ✅
- **نوع البدء**: Automatic ✅
- **يعني**: سيبدأ تلقائياً عند تشغيل الجهاز

### Backend (Port 5000)
- **الحالة**: Running ✅
- **Process ID**: 4
- **متصل بقاعدة البيانات**: نعم ✅

### Frontend (Port 3000)
- **الحالة**: Running ✅
- **Process ID**: 2

---

## 🔧 كيف تحافظ على تشغيل الخدمات

### 1. PostgreSQL (قاعدة البيانات)

#### التحقق من الحالة:
```powershell
Get-Service postgresql-x64-18
```

#### إذا توقفت، شغلها:
```powershell
Start-Service postgresql-x64-18
```

#### التأكد من البدء التلقائي:
```powershell
Set-Service postgresql-x64-18 -StartupType Automatic
```

---

### 2. Backend (Node.js)

#### التحقق من الحالة:
```powershell
# اختبر الـ API
curl http://localhost:5000/api/health
```

#### إذا توقف، شغله:
```powershell
cd backend
npm run dev
```

**ملاحظة**: Backend يعمل في terminal - إذا أغلقت terminal سيتوقف!

---

### 3. Frontend (React)

#### التحقق من الحالة:
```powershell
# افتح في المتصفح
Start-Process http://localhost:3000
```

#### إذا توقف، شغله:
```powershell
npm run dev
```

**ملاحظة**: Frontend يعمل في terminal - إذا أغلقت terminal سيتوقف!

---

## ⚠️ أسباب توقف الخدمات

### PostgreSQL قد يتوقف إذا:
- ❌ أغلقت الجهاز بشكل مفاجئ
- ❌ حدث خطأ في النظام
- ❌ نفذت أمر Stop-Service

**الحل**: PostgreSQL مضبوط على Automatic - سيبدأ تلقائياً

### Backend قد يتوقف إذا:
- ❌ أغلقت terminal
- ❌ حدث خطأ في الكود
- ❌ قاعدة البيانات توقفت
- ❌ Port 5000 مستخدم من برنامج آخر

**الحل**: شغله مرة أخرى بـ `npm run dev`

### Frontend قد يتوقف إذا:
- ❌ أغلقت terminal
- ❌ حدث خطأ في الكود
- ❌ Port 3000 مستخدم من برنامج آخر

**الحل**: شغله مرة أخرى بـ `npm run dev`

---

## 🚀 تشغيل كل شيء مرة واحدة

### الطريقة السريعة:

```powershell
# 1. تأكد من PostgreSQL
Start-Service postgresql-x64-18

# 2. شغل Backend (في terminal جديد)
cd backend
npm run dev

# 3. شغل Frontend (في terminal آخر)
cd ..
npm run dev

# 4. افتح المتصفح
Start-Process http://localhost:3000
```

---

## 📊 مراقبة الخدمات

### التحقق من PostgreSQL:
```powershell
Get-Service postgresql-x64-18 | Select-Object Status, StartType
```

### التحقق من Backend:
```powershell
curl http://localhost:5000/api/health
```

### التحقق من Frontend:
```powershell
# افتح في المتصفح
http://localhost:3000
```

---

## 🔄 إعادة التشغيل التلقائي

### لـ PostgreSQL:
✅ **مضبوط بالفعل** - يبدأ تلقائياً عند تشغيل الجهاز

### لـ Backend و Frontend:
⚠️ **يجب تشغيلهما يدوياً** بعد كل إعادة تشغيل للجهاز

**البديل**: استخدام PM2 (Process Manager)

```powershell
# تثبيت PM2
npm install -g pm2

# تشغيل Backend
cd backend
pm2 start "npm run dev" --name waqif-backend

# تشغيل Frontend
cd ..
pm2 start "npm run dev" --name waqif-frontend

# حفظ الإعدادات
pm2 save

# تشغيل PM2 عند بدء النظام
pm2 startup
```

---

## 🛠️ استكشاف الأخطاء

### PostgreSQL لا يعمل:
```powershell
# تحقق من الحالة
Get-Service postgresql-x64-18

# شغله
Start-Service postgresql-x64-18

# تحقق من الـ logs
# في: C:\Program Files\PostgreSQL\18\data\log\
```

### Backend لا يعمل:
```powershell
# تحقق من Port 5000
netstat -ano | findstr :5000

# إذا كان مستخدم، أوقف البرنامج أو غير Port
```

### Frontend لا يعمل:
```powershell
# تحقق من Port 3000
netstat -ano | findstr :3000

# إذا كان مستخدم، أوقف البرنامج أو غير Port
```

---

## 📝 ملاحظات مهمة

### PostgreSQL:
- ✅ يعمل كـ Windows Service
- ✅ يبدأ تلقائياً
- ✅ لا يحتاج terminal مفتوح

### Backend & Frontend:
- ⚠️ يعملان في terminal
- ⚠️ يتوقفان عند إغلاق terminal
- ⚠️ يجب تشغيلهما يدوياً بعد إعادة تشغيل الجهاز

---

## 🎯 التوصيات

### للتطوير اليومي:
1. ✅ اترك PostgreSQL يعمل دائماً (Automatic)
2. ✅ شغل Backend و Frontend عند الحاجة
3. ✅ استخدم terminals منفصلة لكل واحد

### للإنتاج:
1. ✅ استخدم PM2 لإدارة Backend و Frontend
2. ✅ أو انشر على الاستضافة (Railway + Vercel)
3. ✅ استخدم قاعدة بيانات سحابية

---

## ✅ الخلاصة

**الآن:**
- ✅ PostgreSQL: يعمل ومضبوط على Automatic
- ✅ Backend: يعمل (Process ID: 4)
- ✅ Frontend: يعمل (Process ID: 2)

**إذا أغلقت الجهاز:**
- ✅ PostgreSQL سيبدأ تلقائياً
- ⚠️ Backend و Frontend تحتاج تشغيل يدوي

**الأوامر السريعة:**
```powershell
# Backend
cd backend
npm run dev

# Frontend (في terminal آخر)
npm run dev
```

---

تاريخ: 2024-12-01
