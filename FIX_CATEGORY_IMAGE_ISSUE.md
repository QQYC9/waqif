# 🖼️ إصلاح مشكلة صورة "معامل تعليمية"

## ❌ المشكلة

صورة قسم "معامل تعليمية" لا تظهر في الصفحة الرئيسية.

## 🔍 السبب

المشكلة **ليست في الصورة نفسها**، بل في أن القسم **لم يكن معروضاً** في الصفحة الرئيسية!

### التفاصيل:

في `pages/HomePage.tsx`، كان الكود يعرض فقط الفئات ذات IDs: `[1, 2, 4, 5]`

```typescript
// ❌ الكود القديم
const mainCategoryIds = [1, 2, 4, 5];
```

لكن الفئات الحالية في قاعدة البيانات لها IDs مختلفة:

| ID | الاسم |
|----|-------|
| 25 | كيماويات ومواد كيميائية |
| 26 | مستلزمات مخبرية طبية |
| 27 | مستلزمات معملية |
| 28 | مستلزمات منظفة طبية |
| 29 | **معامل تعليمية** ← هذا القسم! |

## ✅ الحل

تم تحديث `mainCategoryIds` لتتضمن الـ IDs الصحيحة:

```typescript
// ✅ الكود الجديد
const mainCategoryIds = [25, 26, 27, 29]; // عرض 4 فئات رئيسية
```

### الفئات المعروضة الآن:
1. ✅ كيماويات ومواد كيميائية (25)
2. ✅ مستلزمات مخبرية طبية (26)
3. ✅ مستلزمات معملية (27)
4. ✅ **معامل تعليمية (29)** ← الآن تظهر!

## 🖼️ معلومات الصورة

- **المسار:** `/uploads/image-1764536101658-6257420.jpg`
- **الحجم:** 112,214 bytes (112 KB)
- **الحالة:** ✅ موجودة وتعمل
- **URL الكامل:** `http://localhost:5000/uploads/image-1764536101658-6257420.jpg`

## 🧪 التحقق

### 1. اختبار الصورة مباشرة:
```bash
curl -I http://localhost:5000/uploads/image-1764536101658-6257420.jpg
# النتيجة: HTTP/1.1 200 OK ✅
```

### 2. اختبار API:
```bash
curl http://localhost:5000/api/categories/29
```

**النتيجة:**
```json
{
  "success": true,
  "data": {
    "id": 29,
    "name": "معامل تعليمية",
    "image": "/uploads/image-1764536101658-6257420.jpg"
  }
}
```

### 3. اختبار في المتصفح:
افتح: http://localhost:3002

يجب أن ترى 4 فئات في الصفحة الرئيسية، بما فيها "معامل تعليمية" مع صورتها.

## 🎨 كيف تعمل الصور

### 1. في قاعدة البيانات:
```
image: "/uploads/image-1764536101658-6257420.jpg"
```

### 2. دالة getImageUrl تحولها إلى:
```typescript
// في utils/imageHelper.ts
if (imagePath.startsWith('/uploads')) {
  const backendUrl = 'http://localhost:5000';
  return `${backendUrl}${imagePath}`;
}
// النتيجة: http://localhost:5000/uploads/image-1764536101658-6257420.jpg
```

### 3. في HTML:
```html
<img src="http://localhost:5000/uploads/image-1764536101658-6257420.jpg" alt="معامل تعليمية" />
```

## 📝 ملاحظات

### إذا أردت تغيير الفئات المعروضة:

```typescript
// في pages/HomePage.tsx

// عرض جميع الفئات:
const mainCategories = categories;

// أو عرض فئات محددة:
const mainCategoryIds = [25, 26, 27, 29];
const mainCategories = categories.filter(cat => mainCategoryIds.includes(cat.id));

// أو عرض أول 4 فئات:
const mainCategories = categories.slice(0, 4);
```

### إذا أردت تغيير صورة الفئة:

#### الطريقة 1: من لوحة التحكم
1. اذهب إلى http://localhost:3002/admin
2. ابحث عن "معامل تعليمية"
3. اضغط "تعديل"
4. ارفع صورة جديدة
5. احفظ

#### الطريقة 2: من API مباشرة
```bash
# رفع صورة جديدة
curl -X POST http://localhost:5000/api/upload \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -F "image=@/path/to/new-image.jpg"

# تحديث الفئة
curl -X PUT http://localhost:5000/api/categories/29 \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"image": "/uploads/new-image.jpg"}'
```

## 🔄 Cache

إذا لم تظهر الصورة فوراً، قد يكون بسبب الـ Cache:

### مسح Cache في المتصفح:
- **Chrome/Edge:** Ctrl + Shift + R
- **Firefox:** Ctrl + F5

### مسح Cache في Backend:
الـ Cache يُمسح تلقائياً عند تحديث الفئات.

## ✅ الخلاصة

- ✅ الصورة موجودة وتعمل
- ✅ تم تحديث HomePage لعرض القسم
- ✅ القسم "معامل تعليمية" يظهر الآن
- ✅ الصورة تُعرض بشكل صحيح

**المشكلة محلولة! 🎉**

---

## 📊 جميع الفئات الحالية

| ID | الاسم | الصورة | الحالة |
|----|-------|--------|--------|
| 25 | كيماويات ومواد كيميائية | `/images/category-chemicals.jpg` | ✅ معروضة |
| 26 | مستلزمات مخبرية طبية | `/images/FB_IMG_1762725738851.jpg` | ✅ معروضة |
| 27 | مستلزمات معملية | `/images/category-lab-equipment.jpg` | ✅ معروضة |
| 28 | مستلزمات منظفة طبية | `/images/slider-1.jpg` | ❌ غير معروضة |
| 29 | معامل تعليمية | `/uploads/image-1764536101658-6257420.jpg` | ✅ معروضة |

**ملاحظة:** إذا أردت عرض "مستلزمات منظفة طبية" أيضاً، أضف 28 إلى `mainCategoryIds`:
```typescript
const mainCategoryIds = [25, 26, 27, 28, 29]; // عرض جميع الفئات
```
