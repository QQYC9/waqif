# نظرة شاملة على مشروع موقع واقف إنترناشيونال

## 📋 معلومات عامة

**اسم المشروع:** موقع شركة واقف إنترناشيونال  
**الوصف:** موقع إلكتروني لشركة متخصصة في المستلزمات الطبية والمواد الكيماوية وتجهيز المختبرات  
**التقنيات المستخدمة:** React 19, TypeScript, Vite, Tailwind CSS, React Router  
**اللغة:** العربية (RTL)

---

## 🏗️ البنية العامة للمشروع

### 1. **الملفات الأساسية (Root Files)**

#### `package.json`
- **التبعيات:**
  - `react` ^19.2.0
  - `react-dom` ^19.2.0
  - `react-router-dom` ^7.9.5
- **التبعيات التطويرية:**
  - `vite` ^6.2.0
  - `typescript` ~5.8.2
  - `@vitejs/plugin-react` ^5.0.0

#### `vite.config.ts`
- خادم التطوير على المنفذ 3000
- دعم متغيرات البيئة (GEMINI_API_KEY)
- إعدادات المسارات (Aliases)

#### `index.html`
- HTML الأساسي مع دعم RTL
- استخدام Tailwind CSS من CDN
- خط Tajawal للعربية
- React من CDN (importmap)

#### `index.tsx`
- نقطة الدخول الرئيسية
- استخدام HashRouter للتنقل
- React StrictMode مفعّل

---

## 📁 هيكل المجلدات

```
├── components/          # المكونات القابلة لإعادة الاستخدام
│   ├── icons/          # الأيقونات المخصصة
│   ├── Header.tsx      # رأس الصفحة
│   ├── Footer.tsx      # تذييل الصفحة
│   ├── Sidebar.tsx     # القائمة الجانبية
│   ├── HeroSlider.tsx  # سلايدر الصور الرئيسية
│   ├── ProductCard.tsx # بطاقة المنتج
│   └── LanguageSelector.tsx # محدد اللغة
├── pages/              # صفحات الموقع
│   ├── HomePage.tsx
│   ├── AboutPage.tsx
│   ├── ServicesPage.tsx
│   ├── ContactPage.tsx
│   ├── ProductsListPage.tsx
│   ├── CategoryProductsPage.tsx
│   ├── ProductDetailPage.tsx
│   └── AdminDashboardPage.tsx
├── public/
│   └── images/         # الصور والموارد الثابتة
├── types.ts           # تعريفات TypeScript
├── data.ts            # البيانات الأولية
└── App.tsx            # المكون الرئيسي

```

---

## 🧩 المكونات (Components)

### 1. **Header.tsx**
- **الوظيفة:** رأس الصفحة الثابت
- **المميزات:**
  - شعار الشركة
  - زر القائمة (Hamburger)
  - محدد اللغة
  - ثابت في الأعلى (fixed)
- **الارتفاع:** 24 (96px)

### 2. **Footer.tsx**
- **الوظيفة:** تذييل الصفحة
- **المحتوى:**
  - معلومات الشركة
  - روابط سريعة
  - معلومات الاتصال (عنوان، بريد، هاتف، واتساب)
  - حقوق النشر

### 3. **Sidebar.tsx**
- **الوظيفة:** قائمة جانبية منزلقة
- **المميزات:**
  - Accordion للفئات والأقسام الفرعية
  - عرض المنتجات بشكل هرمي
  - إغلاق تلقائي عند النقر على رابط
  - خلفية داكنة (teal-700)

### 4. **HeroSlider.tsx** ⭐
- **الوظيفة:** سلايدر الصور الرئيسية
- **المميزات:**
  - صور متجاوبة (Responsive Images)
  - 3 صور مختلفة
  - تغيير تلقائي كل 5 ثوان
  - نقاط تنقل تفاعلية
  - أزرار سابقة/تالية (Desktop فقط)
  - **نظام التسمية:**
    - `Hero for PC{number}.jpg` (1920×600-800px)
    - `Hero for Tablet{number}.jpg` (1024×400-500px)
    - `Hero for Mobile{number}.jpg` (414×200-250px)

### 5. **ProductCard.tsx**
- **الوظيفة:** بطاقة عرض المنتج
- **التصميم:** صورة دائرية مع عنوان

### 6. **LanguageSelector.tsx**
- **الوظيفة:** محدد اللغة
- **الحالة:** واجهة فقط (غير مفعّل حالياً)

---

## 📄 الصفحات (Pages)

### 1. **HomePage.tsx**
- **المحتوى:**
  - Hero Slider
  - قسم "منتجاتنا الرئيسية" (4 فئات رئيسية)
  - بانر ترويجي
  - معلومات الشركة
  - قسم "لماذا تختارنا"
  - قسم "رؤيتنا"

### 2. **ProductsListPage.tsx**
- **الوظيفة:** عرض جميع المنتجات منظمة حسب الفئات
- **التصميم:** شبكة من البطاقات الدائرية

### 3. **CategoryProductsPage.tsx**
- **الوظيفة:** عرض منتجات فئة محددة
- **المميزات:**
  - عرض الأقسام الفرعية
  - عرض المنتجات في كل قسم فرعي
  - تصميم شبكي

### 4. **ProductDetailPage.tsx**
- **الوظيفة:** صفحة تفاصيل المنتج
- **المحتوى:**
  - صورة المنتج
  - الاسم والسعر
  - الوصف
  - المواصفات
  - زر "اطلب الآن / استفسر"

### 5. **AboutPage.tsx**
- **المحتوى:** معلومات عن الشركة

### 6. **ServicesPage.tsx**
- **المحتوى:** عرض الخدمات المقدمة
- **4 خدمات رئيسية:**
  - تجهيز المختبرات المتكامل
  - توريد المواد الكيميائية
  - صيانة ومعايرة الأجهزة
  - استشارات فنية وتدريب

### 7. **ContactPage.tsx**
- **المحتوى:**
  - معلومات الاتصال
  - ساعات العمل
  - نموذج تواصل (غير مفعّل حالياً)

### 8. **AdminDashboardPage.tsx** 🔧
- **الوظيفة:** لوحة تحكم لإدارة المحتوى
- **المميزات:**
  - إضافة/حذف الفئات الرئيسية
  - إضافة/حذف الأقسام الفرعية
  - إضافة/حذف المنتجات
  - **ملاحظة:** التغييرات في الذاكرة فقط (لا يتم حفظها)

---

## 📊 البيانات (Data Structure)

### `types.ts`
```typescript
- Product: id, subCategoryId, name, image, description, specifications, price
- SubCategory: id, categoryId, name
- Category: id, name, image
```

### `data.ts`
- **5 فئات رئيسية:**
  1. كيماويات وأوساط زراعية
  2. مستلزمات ومعدات طبية
  3. مستلزمات مخبرية
  4. أجهزة ومعدات قياس
  5. معامل تعليمية

- **13 قسم فرعي**
- **~40 منتج** موزعة على الأقسام

---

## 🎨 التصميم والأنماط

### **الألوان الرئيسية:**
- **Teal:** #00b3b3 (اللون الأساسي)
- **Slate:** للعناوين والنصوص
- **Gray:** للخلفيات والنصوص الثانوية

### **الخطوط:**
- **Tajawal:** خط عربي احترافي

### **Responsive Breakpoints:**
- Mobile: < 768px
- Tablet: 768px - 1024px
- Desktop: > 1024px

---

## 🔄 التوجيه (Routing)

### **المسارات:**
- `/` - الصفحة الرئيسية
- `/about` - من نحن
- `/services` - خدماتنا
- `/contact` - تواصل معنا
- `/products` - قائمة المنتجات
- `/category/:categoryId` - منتجات فئة محددة
- `/product/:productId` - تفاصيل منتج
- `/admin` - لوحة التحكم

**ملاحظة:** استخدام HashRouter (`#/`)

---

## 📸 الصور والموارد

### **موقع الصور:**
`public/images/`

### **صور الهيرو:**
- `Hero for PC1.jpg`, `Hero for PC2.jpg`, `Hero for PC3.jpg`
- `Hero for Tablet1.jpg`, `Hero for Tablet2.jpg`, `Hero for Tablet3.jpg`
- `Hero for Mobile1.jpg`, `Hero for Mobile2.jpg`, `Hero for Mobile3.jpg`

### **صور أخرى:**
- `logo.png` - شعار الشركة
- `category-*.jpg` - صور الفئات
- `product-*.jpg` - صور المنتجات
- `slider-*.jpg` - صور إضافية
- `promo-banner.jpg` - البانر الترويجي

---

## ⚙️ الحالة (State Management)

### **إدارة الحالة:**
- استخدام React Hooks (useState)
- الحالة في `App.tsx`:
  - `categories` - الفئات
  - `subCategories` - الأقسام الفرعية
  - `products` - المنتجات
  - `isSidebarOpen` - حالة القائمة الجانبية

### **تمرير البيانات:**
- تمرير البيانات عبر Props
- لا يوجد Context API أو State Management Library

---

## 🚀 التشغيل والبناء

### **الأوامر:**
```bash
npm run dev      # تشغيل خادم التطوير
npm run build    # بناء المشروع للإنتاج
npm run preview  # معاينة البناء
```

### **المنفذ:**
- التطوير: `http://localhost:3000`

---

## 🔍 المميزات الرئيسية

### ✅ **مكتملة:**
1. تصميم متجاوب (Responsive)
2. صور هيرو متجاوبة حسب الجهاز
3. نظام تنقل كامل
4. عرض المنتجات والفئات
5. لوحة تحكم أساسية
6. تصميم احترافي وعربي

### ⚠️ **تحتاج تحسين:**
1. محدد اللغة (غير مفعّل)
2. نموذج التواصل (غير مفعّل)
3. حفظ البيانات (حالياً في الذاكرة فقط)
4. البحث والفلترة
5. نظام إدارة الصور

---

## 📝 ملاحظات مهمة

1. **التخزين:** البيانات حالياً في الذاكرة فقط، التغييرات تُفقد عند تحديث الصفحة
2. **API:** لا يوجد API خلفي، كل البيانات محلية
3. **التوثيق:** يوجد ملفات توثيق للصور الهيرو
4. **الأداء:** استخدام Lazy Loading للصور
5. **الوصولية:** بعض عناصر ARIA موجودة

---

## 🎯 التوصيات للتطوير المستقبلي

1. **إضافة Backend API** لحفظ البيانات
2. **تفعيل نموذج التواصل** مع إرسال بريد إلكتروني
3. **إضافة نظام البحث** للمنتجات
4. **تحسين SEO** (Meta tags, Structured Data)
5. **إضافة نظام التقييمات** للمنتجات
6. **تحسين الأداء** (Code Splitting, Image Optimization)
7. **إضافة نظام المصادقة** للوحة التحكم
8. **دعم متعدد اللغات** كامل

---

**آخر تحديث:** تم فهم المشروع بالكامل ✅

