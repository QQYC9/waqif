# دليل مرئي - Visual Guide 🎨

## الهيكل الحالي

```
📦 المشروع
├── 📁 backend/              ← Backend (Node.js + Express)
│   ├── 📁 src/
│   │   ├── server.ts       ← Main server
│   │   ├── 📁 routes/      ← API routes
│   │   ├── 📁 models/      ← Database models
│   │   └── 📁 config/      ← Configuration
│   ├── package.json
│   ├── tsconfig.json
│   └── .env.example
│
├── 📁 components/           ← React components
├── 📁 pages/               ← React pages
├── 📁 services/            ← API client
│   └── api.ts             ← Frontend API calls
├── App.tsx                ← Main app
├── package.json
├── vite.config.ts
└── vercel.json            ← Vercel config
```

---

## الهيكل بعد النشر

```
┌─────────────────────────────────────────────────────────┐
│                      المستخدم                            │
│                   (Web Browser)                         │
└────────────────────┬────────────────────────────────────┘
                     │
                     │ HTTPS
                     ↓
┌─────────────────────────────────────────────────────────┐
│                  Frontend (Vercel)                      │
│  ┌──────────────────────────────────────────────────┐  │
│  │  React + Vite                                     │  │
│  │  - Components                                     │  │
│  │  - Pages                                          │  │
│  │  - Routing                                        │  │
│  └──────────────────────────────────────────────────┘  │
│                                                          │
│  Environment Variables:                                 │
│  VITE_API_URL = https://backend.railway.app/api        │
└────────────────────┬────────────────────────────────────┘
                     │
                     │ API Calls (HTTPS)
                     │ GET /api/products
                     │ GET /api/categories
                     │ POST /api/auth/login
                     ↓
┌─────────────────────────────────────────────────────────┐
│                 Backend (Railway)                       │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Node.js + Express                                │  │
│  │  - Routes (API endpoints)                         │  │
│  │  - Controllers                                    │  │
│  │  - Middleware (Auth, CORS)                        │  │
│  └──────────────────────────────────────────────────┘  │
│                                                          │
│  Environment Variables:                                 │
│  DATABASE_URL = postgres://...                          │
│  JWT_SECRET = ...                                       │
│  PORT = 5000                                            │
└────────────────────┬────────────────────────────────────┘
                     │
                     │ SQL Queries
                     │ SELECT * FROM Products
                     │ INSERT INTO Categories
                     ↓
┌─────────────────────────────────────────────────────────┐
│              PostgreSQL Database (Railway)              │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Tables:                                          │  │
│  │  - Products (797 rows)                            │  │
│  │  - Categories (5 rows)                            │  │
│  │  - SubCategories (22 rows)                        │  │
│  │  - Users (1 row)                                  │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

---

## تدفق البيانات (Data Flow)

### 1. المستخدم يفتح الموقع

```
المستخدم
   │
   │ يفتح https://your-site.vercel.app
   ↓
Vercel CDN
   │
   │ يرسل HTML + CSS + JS
   ↓
المتصفح
   │
   │ يحمّل React App
   ↓
Frontend يعمل ✅
```

### 2. Frontend يطلب البيانات

```
Frontend (React)
   │
   │ useEffect(() => {
   │   fetchProducts();
   │ })
   ↓
services/api.ts
   │
   │ axios.get(VITE_API_URL + '/products')
   ↓
Backend API
   │
   │ GET https://backend.railway.app/api/products
   ↓
Backend يستقبل الطلب ✅
```

### 3. Backend يجلب من Database

```
Backend (Express)
   │
   │ app.get('/api/products', ...)
   ↓
Controller
   │
   │ Product.findAll()
   ↓
Sequelize ORM
   │
   │ SELECT * FROM "Products"
   ↓
PostgreSQL
   │
   │ يرجع 797 منتج
   ↓
Backend يرسل JSON ✅
```

### 4. Frontend يعرض البيانات

```
Backend Response
   │
   │ { success: true, data: [...] }
   ↓
Frontend
   │
   │ setProducts(data)
   ↓
React Re-render
   │
   │ products.map(p => <ProductCard />)
   ↓
المستخدم يرى المنتجات ✅
```

---

## خريطة API Endpoints

```
Backend API (https://backend.railway.app/api)
│
├── /health                    GET    ← Health check
│
├── /auth
│   ├── /login                POST   ← تسجيل دخول
│   ├── /register             POST   ← تسجيل جديد
│   └── /me                   GET    ← معلومات المستخدم
│
├── /categories
│   ├── /                     GET    ← جميع الفئات
│   ├── /:id                  GET    ← فئة واحدة
│   ├── /                     POST   ← إضافة فئة (Admin)
│   ├── /:id                  PUT    ← تعديل فئة (Admin)
│   └── /:id                  DELETE ← حذف فئة (Admin)
│
├── /subcategories
│   ├── /                     GET    ← جميع الفئات الفرعية
│   ├── /:id                  GET    ← فئة فرعية واحدة
│   ├── /                     POST   ← إضافة (Admin)
│   ├── /:id                  PUT    ← تعديل (Admin)
│   └── /:id                  DELETE ← حذف (Admin)
│
├── /products
│   ├── /                     GET    ← جميع المنتجات (مع بحث)
│   ├── /:id                  GET    ← منتج واحد
│   ├── /                     POST   ← إضافة منتج (Admin)
│   ├── /:id                  PUT    ← تعديل منتج (Admin)
│   └── /:id                  DELETE ← حذف منتج (Admin)
│
└── /upload
    └── /                     POST   ← رفع صورة (Admin)
```

---

## متغيرات البيئة (Environment Variables)

### Frontend (Vercel)

```
┌─────────────────────────────────────┐
│  VITE_API_URL                       │
│  ↓                                  │
│  يُستخدم في services/api.ts        │
│  ↓                                  │
│  const API_URL = import.meta.env    │
│                 .VITE_API_URL       │
│  ↓                                  │
│  axios.create({ baseURL: API_URL }) │
└─────────────────────────────────────┘
```

### Backend (Railway)

```
┌─────────────────────────────────────┐
│  DATABASE_URL                       │
│  ↓                                  │
│  يُستخدم في config/database.ts     │
│  ↓                                  │
│  new Sequelize(DATABASE_URL)        │
│  ↓                                  │
│  الاتصال بـ PostgreSQL              │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  JWT_SECRET                         │
│  ↓                                  │
│  يُستخدم في middleware/auth.ts     │
│  ↓                                  │
│  jwt.sign(payload, JWT_SECRET)      │
│  ↓                                  │
│  إنشاء والتحقق من Tokens           │
└─────────────────────────────────────┘
```

---

## خطوات النشر بالصور

### الخطوة 1: Railway Backend

```
┌─────────────────────────────────────┐
│  Railway Dashboard                  │
│  ┌───────────────────────────────┐ │
│  │  + New                        │ │
│  │    ├─ Empty Service           │ │
│  │    ├─ GitHub Repo         ✓  │ │
│  │    └─ Database                │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │  waqif-backend                │ │
│  │  ├─ Settings                  │ │
│  │  │  ├─ Root Directory: backend│ │
│  │  │  └─ Build: npm run build   │ │
│  │  ├─ Variables                 │ │
│  │  │  ├─ PORT=5000              │ │
│  │  │  ├─ DATABASE_URL=...       │ │
│  │  │  └─ JWT_SECRET=...         │ │
│  │  └─ Networking                │ │
│  │     └─ Generate Domain    ✓   │ │
│  └───────────────────────────────┘ │
└─────────────────────────────────────┘
```

### الخطوة 2: Vercel Frontend

```
┌─────────────────────────────────────┐
│  Vercel Dashboard                   │
│  ┌───────────────────────────────┐ │
│  │  Add New...                   │ │
│  │    └─ Project             ✓  │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │  Import Git Repository        │ │
│  │  ├─ Framework: Vite           │ │
│  │  ├─ Root: ./                  │ │
│  │  ├─ Build: npm run build      │ │
│  │  └─ Output: dist              │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │  Environment Variables        │ │
│  │  ├─ VITE_API_URL              │ │
│  │  │  = https://backend...      │ │
│  │  └─ GEMINI_API_KEY (optional) │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │  [Deploy]                 ✓   │ │
│  └───────────────────────────────┘ │
└─────────────────────────────────────┘
```

---

## الاختبار (Testing)

### 1. اختبار Backend

```
┌─────────────────────────────────────┐
│  المتصفح                            │
│  ┌───────────────────────────────┐ │
│  │  https://backend.railway.app  │ │
│  │         /api/health           │ │
│  └───────────────────────────────┘ │
│                                     │
│  ✅ Response:                       │
│  {                                  │
│    "success": true,                 │
│    "message": "Server is running"   │
│  }                                  │
└─────────────────────────────────────┘
```

### 2. اختبار Frontend

```
┌─────────────────────────────────────┐
│  المتصفح                            │
│  ┌───────────────────────────────┐ │
│  │  https://your-site.vercel.app │ │
│  └───────────────────────────────┘ │
│                                     │
│  ✅ الصفحة تفتح                    │
│  ✅ المنتجات تظهر                  │
│  ✅ البحث يعمل                     │
│  ✅ لا أخطاء في Console            │
└─────────────────────────────────────┘
```

### 3. اختبار الاتصال

```
┌─────────────────────────────────────┐
│  Console (F12)                      │
│  ┌───────────────────────────────┐ │
│  │  Network Tab                  │ │
│  │  ├─ GET /api/products     200 │ │
│  │  ├─ GET /api/categories   200 │ │
│  │  └─ GET /api/subcategories 200│ │
│  └───────────────────────────────┘ │
│                                     │
│  ✅ جميع الطلبات ناجحة             │
│  ✅ لا أخطاء CORS                  │
└─────────────────────────────────────┘
```

---

## الخلاصة

```
✅ قاعدة البيانات → Railway (جاهزة)
⏳ Backend → Railway (جاهز للرفع)
⏳ Frontend → Vercel (جاهز للرفع)

👉 ابدأ من START_HERE.md
```
