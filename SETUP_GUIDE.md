# دليل الإعداد والتشغيل
## موقع واقف إنترناشيونال - من النموذج الأولي إلى المنتج النهائي

---

## 📋 المتطلبات

### Backend:
- Node.js 18+
- MongoDB (محلي أو MongoDB Atlas)
- npm أو yarn

### Frontend:
- Node.js 18+
- npm أو yarn

---

## 🚀 خطوات الإعداد

### 1. إعداد Backend

#### 1.1 الانتقال إلى مجلد Backend
```bash
cd backend
```

#### 1.2 تثبيت التبعيات
```bash
npm install
```

#### 1.3 إعداد متغيرات البيئة
انسخ ملف `.env.example` إلى `.env`:
```bash
# في Windows
copy .env.example .env

# في Linux/Mac
cp .env.example .env
```

ثم عدّل ملف `.env`:
```env
PORT=5000
NODE_ENV=development
MONGODB_URI=mongodb://localhost:27017/waqif-international
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=5242880
```

#### 1.4 تشغيل MongoDB

**خيار 1: MongoDB محلي**
- تأكد من تثبيت MongoDB على جهازك
- شغّل MongoDB service

**خيار 2: MongoDB Atlas (موصى به)**
1. أنشئ حساب على [MongoDB Atlas](https://www.mongodb.com/cloud/atlas)
2. أنشئ Cluster جديد
3. احصل على Connection String
4. ضعه في `MONGODB_URI` في ملف `.env`

#### 1.5 ملء قاعدة البيانات بالبيانات الأولية
```bash
npm run seed
```

سيتم إنشاء:
- مستخدم admin (username: `admin`, password: `admin123`)
- 5 فئات رئيسية
- 13 قسم فرعي
- 3 منتجات تجريبية

⚠️ **مهم:** غيّر كلمة مرور admin بعد أول تسجيل دخول!

#### 1.6 تشغيل Backend
```bash
npm run dev
```

الخادم سيعمل على: `http://localhost:5000`

---

### 2. إعداد Frontend

#### 2.1 العودة إلى المجلد الرئيسي
```bash
cd ..
```

#### 2.2 تثبيت التبعيات
```bash
npm install
```

#### 2.3 إعداد متغيرات البيئة
أنشئ ملف `.env` في المجلد الرئيسي:
```env
VITE_API_URL=http://localhost:5000/api
```

#### 2.4 تشغيل Frontend
```bash
npm run dev
```

الموقع سيعمل على: `http://localhost:3000`

---

## 🔐 تسجيل الدخول

1. افتح المتصفح على: `http://localhost:3000`
2. اذهب إلى: `http://localhost:3000/#/login`
3. استخدم بيانات الدخول:
   - **Username:** `admin`
   - **Password:** `admin123`

---

## 📁 هيكل المشروع

```
├── backend/              # Backend API
│   ├── src/
│   │   ├── config/      # إعدادات
│   │   ├── controllers/ # Controllers
│   │   ├── middleware/  # Middleware
│   │   ├── models/      # MongoDB Models
│   │   ├── routes/      # Routes
│   │   ├── scripts/     # Scripts
│   │   └── server.ts    # نقطة البداية
│   ├── uploads/         # الصور المرفوعة
│   └── package.json
│
├── components/           # مكونات React
├── pages/               # صفحات الموقع
├── services/            # API services
├── contexts/            # React Contexts
└── package.json
```

---

## 🧪 اختبار النظام

### 1. اختبار Backend
```bash
# في مجلد backend
curl http://localhost:5000/api/health
```

يجب أن ترى:
```json
{
  "success": true,
  "message": "Server is running",
  "timestamp": "..."
}
```

### 2. اختبار Frontend
- افتح `http://localhost:3000`
- تأكد من تحميل البيانات من Backend
- جرب تسجيل الدخول

---

## 🐛 استكشاف الأخطاء

### خطأ: "Cannot connect to MongoDB"
- تأكد من تشغيل MongoDB
- تحقق من `MONGODB_URI` في `.env`
- تأكد من أن MongoDB يستمع على المنفذ الصحيح

### خطأ: "Network Error" في Frontend
- تأكد من تشغيل Backend على `http://localhost:5000`
- تحقق من `VITE_API_URL` في `.env`
- تأكد من CORS في Backend

### خطأ: "401 Unauthorized"
- تأكد من تسجيل الدخول
- تحقق من Token في localStorage
- جرب تسجيل الدخول مرة أخرى

### خطأ: "Cannot find module"
- احذف `node_modules` و `package-lock.json`
- شغّل `npm install` مرة أخرى

---

## 📝 ملاحظات مهمة

1. **البيئة:** استخدم `.env` للمتغيرات الحساسة
2. **الأمان:** غيّر `JWT_SECRET` في الإنتاج
3. **النسخ الاحتياطي:** أعد نسخ احتياطية لقاعدة البيانات بانتظام
4. **الصور:** الصور المرفوعة تُحفظ في `backend/uploads/`
5. **المنافذ:** تأكد من أن المنافذ 3000 و 5000 متاحة

---

## 🚀 النشر للإنتاج

### Backend:
1. غيّر `NODE_ENV=production`
2. استخدم MongoDB Atlas
3. غيّر `JWT_SECRET` إلى قيمة قوية
4. استخدم `npm run build` ثم `npm start`

### Frontend:
1. غيّر `VITE_API_URL` إلى URL الإنتاج
2. استخدم `npm run build`
3. انشر مجلد `dist/` على خادم الويب

---

## ✅ قائمة التحقق

- [ ] MongoDB يعمل
- [ ] Backend يعمل على port 5000
- [ ] Frontend يعمل على port 3000
- [ ] البيانات الأولية تم إنشاؤها
- [ ] تسجيل الدخول يعمل
- [ ] لوحة التحكم محمية
- [ ] CRUD operations تعمل
- [ ] رفع الصور يعمل

---

**جاهز للاستخدام! 🎉**

